# Postgres (Railway: usa la referencia ${{Postgres.DATABASE_URL}}; en local, la DATABASE_PUBLIC_URL de Railway o tu Postgres local)
DATABASE_URL="postgresql://usuario:password@host:5432/cromodata"
# Personas simuladas que rellenan la sala (0 = solo respuestas reales)
ROOM_BASELINE=120
# Contraseña de la vista /admin
ADMIN_PASSWORD="cambia-esta-clave"
